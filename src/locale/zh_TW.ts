import messages from '@/languages/messages_zh_TW.json';

import type { Translation } from './types';

const translation: Translation = {
  locale: 'zh_TW',
  messages,
};

export default translation;
